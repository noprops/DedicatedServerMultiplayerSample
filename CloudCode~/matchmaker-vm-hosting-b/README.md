# matchmaker-vm-hosting-b

Cloud Code module for rollout slot `B`.

Secret names:

- `DSMS_VM_B_LAUNCHER_BASE_URL`
- `DSMS_VM_B_LAUNCHER_TOKEN`

Matchmaker pool hosting:

- `moduleName: "MatchmakerVmHostingB"`
- `allocateFunctionName: "allocate"`
- `pollFunctionName: "poll"`
