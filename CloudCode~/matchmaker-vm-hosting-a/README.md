# matchmaker-vm-hosting-a

Cloud Code module for rollout slot `A`.

Secret names:

- `DSMS_VM_A_LAUNCHER_BASE_URL`
- `DSMS_VM_A_LAUNCHER_TOKEN`

Matchmaker pool hosting:

- `moduleName: "MatchmakerVmHostingA"`
- `allocateFunctionName: "allocate"`
- `pollFunctionName: "poll"`
